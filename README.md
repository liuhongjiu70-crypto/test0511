# Matplotlib 折线图示例

这是一个简单的 Python 可视化示例，使用 Matplotlib 绘制 2026 年上半年每月销售额的折线图。

## 安装依赖

```bash
python -m pip install -r requirements.txt
```

## 运行程序

```bash
python line_chart.py
```

运行后会弹出折线图窗口，并在当前目录保存一张 `monthly_sales_line_chart.png` 图片。
